services:
  - type: web
    name: dj-bpm-analyzer
    env: python
    plan: free
    buildCommand: pip install -r requirements_clean.txt
    startCommand: uvicorn app_working:app --host 0.0.0.0 --port 10000
